# ServiceAccount used by runner Job pods. It must exist in every namespace
# that hosts CheckSuites; this manifest covers the operator namespace only.
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/name: verikube
    app.kubernetes.io/managed-by: kustomize
  name: verikube-runner
  namespace: system
