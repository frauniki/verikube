{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "verikube.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "verikube.labels" . | nindent 4 }}
{{- end }}
